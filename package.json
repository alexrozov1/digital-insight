{
  "name": "digital-insight",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && npm run postbuild",
    "postbuild": "npx pagefind --site dist "postbuild": "npx pagefind --site dist""postbuild": "npx pagefind --site dist" cp _worker.js dist/_worker.js",
    "preview": "astro preview",
    "format": "prettier --write ."
  },
  "dependencies": {
    "astro": "^4.10.0",
    "@astrojs/tailwind": "^5.1.0",
    "@astrojs/rss": "^4.0.7",
    "@astrojs/sitemap": "^3.1.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.41",
    "prettier": "^3.3.3",
    "tailwindcss": "^3.4.7",
    "pagefind": "^1.1.1"
  }
}
